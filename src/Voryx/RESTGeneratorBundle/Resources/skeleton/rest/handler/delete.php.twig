{% if document %}
    /**
     * @param {{ entity }} ${{ entity|lower }}
     * @return null
     * @throws \RuntimeException
     */
{% endif %}
{% block method_definition %}
    public function delete({{ entity }} ${{ entity|lower }})
{% endblock method_definition %}
    {
{% block method_body %}
        try {
            $this->om->remove(${{ entity|lower }});
            $this->om->flush();

            return null;
        } catch (\Exception $e) {
            throw new \RuntimeException();
        }
{% endblock method_body %}
    }

