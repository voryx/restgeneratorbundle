
    /**
     * Create a {{ entity }} entity and nest it under the given {{ parent }} entity.
     *
     * @Post("/{{ parent_route }}/{id}/{{ route_prefix }}")
     * @View(statusCode=201, serializerEnableMaxDepthChecks=true)
     * @Secure(roles="{{ roles['all'] }}, {{ roles['create']}}")
     *
     * @param Request $request
     *
     * @return Response
     *
     */
    public function postBy{{ parent }}Action({{ parent }} ${{ parent | lower }}, Request $request)
    {
		$entity = new {{ entity }}();
        $entity->set{{ parent }}(${{ parent | lower }});
        return $this->post($request, get_class(new {{ entity }}Type()), $entity);
    }
