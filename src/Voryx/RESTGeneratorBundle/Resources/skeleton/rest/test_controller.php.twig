<?php

namespace {{ target_namespace }}\Tests\Controller\Generated;

use Symfony\Bundle\FrameworkBundle\Test\WebTestCase;

/**
 * Test controller for {{ entity }} API.
 */
class {{ entity_class }}RESTControllerTest extends NoIncBaseTestController
{

	/**
	 * Before test methods are ran, this method is called.
	 * Create the objects against which you will test.
	 */
	public function setUp()
	{
		$this->client = static::createClient();
	}

	{% for test in actions %}
    {%- include 'rest/tests/' ~ test ~ 'Test.php.twig' %}
    {% endfor %}

    {% for parent, parent_route in parents %}
        {% for parent_action in parent_actions %}
            {%- include 'rest/tests/' ~ parent_action ~ 'Test.php.twig' %}
        {% endfor %}
    {% endfor %}

}
